## Setup the tests guide

1. Build the solution (Restoring the packages)
2. Update the values in setup.ps1 (Dont not check this into source control)
3. Run setup.ps1
4. Undo the file changes in .git
5. Right click the bin/Debug folder, open in terminal window and run the command .\playwright.ps1 install which will install the playwright browsers for you.



